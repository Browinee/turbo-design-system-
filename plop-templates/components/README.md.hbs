# {{ pascalCase name }}


## Install

| type | command                                        |
| ---- | ---------------------------------------------- |
| npm  | `npm install @turbo-design-system/{{{ dashCase name }}}` |
| yarn | `yarn add @turbo-design-system/{{{ dashCase name }}}`    |
| pnpm | `pnpm add @turbo-design-system/{{{ dashCase name }}}`    |


## Usage

```jsx
import {{{ pascalCase name }}} from `@turbo-design-system/{{{ dashCase name }}}`;

function App() {
	return (
		<{{ pascalCase name }} />
	);
}
```
